[project]
name = "puffer_azuki_tcg"
version = "0.1.0"
description = "Azuki TCG PufferLib/PettingZoo RL training"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pufferlib",
]

[tool.uv.sources]
pufferlib = { path = "../../ext/SkyPufferLib", editable = true }
